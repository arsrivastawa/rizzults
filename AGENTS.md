# gym-bae — Workout Tracker

Full spec lives in spec.md at repo root — read it for the data model, screens, and full iteration plan.

## Current Iteration 2 — Click-Through Prototype (still mock data)
Build only:
- Expo navigation shell (bottom tabs: Routines, Library, History, Settings)
- theme.ts (design tokens — see the hevy-style-dark-ui skill)
- Routines List + Exercise Library List screens, hardcoded mock data
- Basic Settings screen shell

Do NOT build yet: SQLite, forms that actually save, session logging, CSV export.